console.log("Hello world");

let pronoun = ["the", "our"];
let adj = ["great", "big"];
let noun = ["reset", "sister"];

let generarDominio = () => {
  let ipronoun = Math.floor(Math.random() * pronoun.length);
  let iadj = Math.floor(Math.random() * adj.length);
  let inoun = Math.floor(Math.random() * noun.length);

  return;
};